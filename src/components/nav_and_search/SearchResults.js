// Div to render the search results for both the search button, and the category search. Returns PosePreview.js cards, and a separate section for associated Flow.js data

import React from "react";

import "../styles/search-results.scss";

const SearchResults = () => {};

export default SearchResults;
