// reusable component that stores references to individual Poses.js cards in a determined order. User cannot change order of Poses

import React from "react";

import "../styles/pre-built-flows.scss";

const PreBuiltFlows = () => {
	// logic goes here
};

export default PreBuiltFlows;
