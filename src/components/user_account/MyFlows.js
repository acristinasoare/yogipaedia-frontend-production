// User's saved Flow.js data. Requires user to be logged in to render. Link in MyAccount.js dropdown menu.
// Display poses chosen for flow - option to reorder? Each pose opens pose component. Remove from flows option.

import React from "react";

import "../styles/my-flows.scss";

const MyFlows = () => {
	// logic goes here
};

export default MyFlows;
