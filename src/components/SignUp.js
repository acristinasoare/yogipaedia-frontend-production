// Page accessed by link on LogIn modal. Takes user to a SignUp page that POSTS to database, via firebase for account auth

import React from "react";

import "../styles/sign-up.scss";

const SignUp = () => {};

export default SignUp;
