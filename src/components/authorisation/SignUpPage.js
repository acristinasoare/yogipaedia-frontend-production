// renders onClick from button on login modal. includes two areas: SignUp.js box and Taster. Format as row on desktop, column on mobile

import React from "react";

import "../styles/sign-up-page.scss";

const SignUpPage = () => {};

export default SignUpPage;
