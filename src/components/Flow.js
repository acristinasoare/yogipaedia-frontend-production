// reusable component that stores references to individual Poses.js cards in a determined order. User can determine order of the Poses

import React from "react";

import "../styles/flow.scss";

const Flow = () => {
	// logic goes here
};

export default Flow;
